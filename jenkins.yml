identidock:
  build: .
#  expose:
#    - "9090"
  ports:
    - "9090:9090"
  environment:
    ENV: PROD
  links:
      - jenkins_dnmonster_1
      - jenkins_redis_1

jenkins_dnmonster_1:
  image: amouat/dnmonster:1.0

jenkins_redis_1:
  image: redis:3.0
